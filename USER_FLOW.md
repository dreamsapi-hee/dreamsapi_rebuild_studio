# DreamSapi Content Rebuilder — USER_FLOW

## 1. 기본 사용자 흐름

```text
홈
→ 새 프로젝트
→ 자료 입력
→ 소디 분석
→ 키디R 5개 주제
→ N개 선택
→ 현재 주제 T01 작업
   → 로디R
   → 라이R
   → 비지R
   → 멀티R(선택)
   → 체키R
→ T01 최종 완료
→ 다음 선택 주제 T02
→ 반복
→ 프로젝트 완료 또는 보관
```

## 2. 홈 → 새 프로젝트

사용자 행동:
1. `새 프로젝트` 클릭
2. 프로젝트명 입력
3. 원자료를 붙여넣거나 파일 업로드
4. 필요하면 여러 자료 추가
5. `분석 시작` 클릭

앱 동작:
- 새 `projectId` 생성
- 원자료 각각에 `sourceId` 생성
- 원자료 전문을 MASTER SOURCE에 저장
- 소디 단계 활성화

예외:
- 자료가 없으면 분석 시작 불가
- 파일 읽기 실패 시 해당 파일만 오류 표시하고 나머지 자료는 유지
- 프로젝트명 미입력 시 임시 제목 자동 생성 가능

## 3. 소디 단계

사용자:
- 분석 결과와 SOURCE MAP 확인
- 필요 시 수정 요청
- `최종 확정` 또는 `확정하고 키디R로`

앱:
- MASTER SOURCE는 수정 없이 별도 보존
- 소디 결과를 `displayResult`로 저장
- SOURCE MAP을 별도 구조화 데이터로 저장
- `SODI_HANDOFF` 저장

예외:
- 분석 결과가 비어 있으면 다음 단계 비활성화
- 사용자가 원자료를 추가하면 소디 재분석 안내

## 4. 키디R 단계

앱 입력:
- MASTER SOURCE
- SOURCE MAP
- 기존 생성 주제(재진입 시)

GPT 결과:
- 5개 주제만 생성

사용자:
1. 5개 확인
2. 필요하면 `다른 관점 5개`
3. 원하는 주제 N개 체크
4. 각 선택 주제의 제목 후보 5개 확인
5. 제목 선택
6. `선택 주제 진행`

앱:
- 선택 주제마다 독립 `topicId` 유지
- 작업 큐 생성
- 첫 번째 선택 주제를 currentTopic으로 지정

예외:
- 선택 주제 0개면 다음 단계 비활성화
- 다른 5개 생성 시 기존 세트는 history에 보관
- 이미 완료된 주제와 유사하면 경고 가능

## 5. 로디R 단계

앱 입력:
- currentTopic
- MASTER SOURCE
- SOURCE MAP
- KIDI_R_HANDOFF

GPT:
- 추천 구조 먼저 제시
- 원자료와 새 글의 차이 표시
- 각 섹션에 사용할 SOURCE MAP 연결

사용자:
- `추천안 적용`
- 일부 구조 수정
- `저장하고 라이R로`

앱:
- 구조 저장
- 상태 `structure_done`

예외:
- 이전 키디 정보 수정 시 로디 결과가 outdated임을 표시

## 6. 라이R 단계

화면 진입 시:
- 우측 주제 목록 항상 표시
- currentTopic 강조

사용자 옵션:
- 글쓰기 모드
- 플랫폼
- 글 길이
- 톤

기본값:
- AI 추천 모드
- 네이버 블로그
- 보통
- 친근한 전문가

GPT 입력:
- MASTER SOURCE
- SOURCE MAP
- KIDI_R_HANDOFF
- RODI_R_HANDOFF
- 선택 옵션

GPT 출력:
- 완성 블로그 글

사용자:
- 수정
- 재생성
- 최종 확정

앱:
- 상태 `article_done`
- 글 전체 저장
- WRITE_R_HANDOFF 저장

예외:
- 사용자가 특정 부분만 수정 요청 시 전체를 덮어쓰지 않고 새 버전 생성 권장

## 7. 비지R 단계

GPT가 먼저:
- 추천 이미지 수
- 추천 스타일
- 전체 이미지 구성

사용자:
- `추천안 적용`
- 이미지 수 +/-
- 특정 이미지 삭제/추가
- `확정`

앱:
- 상태 `visual_done`

예외:
- 실제 이미지 제작은 MVP에서 필수 아님
- 기획만 확정하고 다음 단계 이동 가능

## 8. 멀티R 단계

사용자:
- 확장할 플랫폼 체크
- 필요 없으면 `건너뛰기`
- `선택한 플랫폼 생성`

GPT 입력:
- MASTER SOURCE
- SOURCE MAP
- 최종 블로그 글
- 비지 결과
- 선택 플랫폼

앱:
- 결과를 플랫폼별 저장
- 상태 `multi_done`

예외:
- 플랫폼 미선택 시 멀티 단계 건너뛰기 허용
- 건너뛰어도 체키R 진행 가능

## 9. 체키R 단계

GPT 입력:
- MASTER SOURCE
- SOURCE MAP
- currentTopic 전체 결과

출력 1:
- 검수 리포트

출력 2:
- FINAL PACKAGE

사용자:
- 결과 확인
- 필요 시 수정 요청
- `최종 완료`

앱:
- 최종본 별도 저장
- 상태 `final_done`
- 완료 시간 기록

## 10. 다음 주제로 이동

현재 주제 완료 후:
- 선택 주제 중 미완료 주제가 있으면 `다음 주제` 버튼 활성화
- 다음 주제의 로디R 단계로 이동

권장 UX:

```text
T01 최종 완료 🎉

다음 작업
T02 슬라이드 수 문제
[다음 주제로]

또는
[키디R로 돌아가기]
```

## 11. 기존 주제 수정

완료 주제 열기:
- `최종 결과 보기`
- `수정 작업 시작`

수정 작업:
- 기존 결과는 version 1로 보존
- 수정할 단계 선택
- 이후 단계는 `outdated` 표시
- 새 확정 시 version 2 저장

## 12. 원자료 추가

작업 도중 새 자료 추가 가능.

앱은 다음 안내:

```text
새 원자료가 추가되었습니다.
소디 SOURCE MAP을 업데이트하면 이후 결과에 영향을 줄 수 있습니다.
[소디 다시 분석] [현재 결과 유지]
```

재분석 시:
- 기존 SOURCE MAP 버전 보존
- 이후 단계 결과는 자동 삭제하지 않음
- outdated 표시만 적용

## 13. 저장/이탈

- 입력 변경 시 dirty 상태 표시
- 단계 이동 전 자동 저장 권장
- 브라우저 새로고침 후 마지막 단계 복원
- 삭제는 항상 확인 모달

## 14. 모바일 흐름

- 왼쪽 단계 메뉴 → 상단/하단 드롭다운 또는 스텝 바
- 오른쪽 프로젝트 패널 → 슬라이드 패널 또는 탭
- 현재 주제 목록은 라이R 이후 쉽게 열 수 있어야 함
